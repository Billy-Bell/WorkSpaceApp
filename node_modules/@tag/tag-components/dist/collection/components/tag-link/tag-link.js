import { createStyleFromJson } from '../../shared/utils';
export class TagLink {
    constructor() {
        /** The accent for the text */
        this.accent = 'viridiangreen';
    }
    render() {
        const className = this.accent ? `tag-link__accent--${this.accent}` : '';
        const text = this.text ? this.text : h("slot", null);
        return (h("a", { href: this.href, target: this.target, class: className, style: createStyleFromJson(this.linkStyle) }, text));
    }
    static get is() { return "tag-link"; }
    static get encapsulation() { return "shadow"; }
    static get properties() { return {
        "accent": {
            "type": String,
            "attr": "accent"
        },
        "href": {
            "type": String,
            "attr": "href"
        },
        "linkStyle": {
            "type": String,
            "attr": "link-style"
        },
        "target": {
            "type": String,
            "attr": "target"
        },
        "text": {
            "type": String,
            "attr": "text"
        }
    }; }
    static get style() { return "/**style-placeholder:tag-link:**/"; }
}
