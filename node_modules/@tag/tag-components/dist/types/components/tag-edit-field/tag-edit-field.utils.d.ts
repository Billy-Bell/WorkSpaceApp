export interface IFieldValidator {
    rule: (values: {
        [key: string]: any;
    }, fieldName: string, args: any) => string | Promise<string>;
    args?: any;
    message?: string;
    executeOn?: 'valueChange' | 'blur';
}
export interface IFieldItem {
    label: string;
    value: any;
    selected: boolean;
}
/**
 * Maps data to IFieldItem[] which is what the field component expects
 * @param {any} data - The data to map
 * @param {string} defaultValue - The default value
 */
export declare const mapItems: (data: any, defaultValue: string) => IFieldItem[];
