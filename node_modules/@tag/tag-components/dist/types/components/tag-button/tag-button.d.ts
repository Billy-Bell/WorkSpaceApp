import '../../stencil.core';
import { Accent, ButtonSize, Icon } from '../../shared/types';
export declare class TagButton {
    /** Colour of the button */
    accent: Accent;
    /** The Size of the button */
    size: ButtonSize;
    /** The type of the button */
    type: 'button' | 'submit' | 'reset';
    /** The icon to display in the button */
    icon: Icon;
    /** The icon accent */
    iconAccent: Accent;
    /** The text to display in the button badge */
    badge: string;
    /** The text to display in the button badge */
    badgeAccent: Accent;
    /** If set to true displays badge inline next to button text, else the badge is displayed top right */
    badgeInline: boolean;
    /** Whether the button is disabled */
    disabled: boolean;
    /** The button minimum width */
    minWidth: string;
    /** The button text */
    text: string;
    /** Whether the button is rounded  */
    rounded: boolean;
    /** Whether the button is squared  */
    squared: boolean;
    /** The reference to the DOM element */
    private element;
    /** Whether the menu items are visible or not */
    private itemsVisible;
    /** Event raised when the button is clicked  */
    private buttonClick;
    /** This holds the elements text content */
    private internalElementTextContent;
    handleKeyDown(e: KeyboardEvent): void;
    componentWillLoad(): void;
    componentDidUnload(): void;
    /**
     * Subscribe to the menuItemClicked event from the menu items.
     * When menu items are clicked we should hide the menu
     */
    menuItemClickHandler(): Promise<any>;
    render(): JSX.Element;
    /** Gets the button text */
    private getText;
    /** Gets the CSS class for the button */
    private getClass;
    /** Whether the button has menu options */
    private haveMenu;
    private hideOptions;
    private handleClick;
    private renderBadge;
    private getIconAccent;
    private renderIconButton;
    private renderTextButton;
}
