export declare enum SortStates {
    unsorted = "unsorted",
    ascending = "ascending",
    descending = "descending"
}
export declare const sortStateMapper: () => Map<string, SortStates>;
interface Sorted {
    state: SortStates;
    data: any[];
}
export declare const sort: (sortState: SortStates, key: string, data: any[]) => Sorted;
export {};
