import { DataType } from '../../shared/types';
export interface IRowButtonClick {
    row: any;
}
export interface IDataTableColumn {
    name: string;
    caption: string;
    sortable?: boolean;
    dataType?: DataType | 'icon' | 'button';
    formattingOptionsObj?: any;
    cellHeight?: string;
    cellWidth?: string;
    cellAccentField?: string;
    cellFieldStyle?: string;
}
