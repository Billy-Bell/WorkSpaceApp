import '../../stencil.core';
import { IHttpResponse } from '../../shared/http';
export declare class TagWizard {
    height: string;
    submitSuccessMessage: string;
    submitFailureMessage: string;
    submitValidationMessage: string;
    /**
     * A function that maps server validation errors to IFormErrors which is the format this component expects.
     * By default the ASP.NET MVC mapper is used
     */
    serverValidationMapper: (response: IHttpResponse, fieldNames: string[]) => {
        [key: string]: string[];
    };
    private activeStepIndex;
    private submitResult;
    /** Reference to the DOM element */
    private element;
    /** Event that can be subscribed to handle the wizard submission */
    private submit;
    /** Gets the child tab components */
    componentDidLoad(): void;
    /** Go to the previous step when teh previous button is clicked */
    handlePreviousClick(): void;
    /** Go to the next step when the next button is clicked */
    handleNextClick(): void;
    /** Submit the data when finish is clicked */
    handleFinishClick(): void;
    submitComplete(response: IHttpResponse): void;
    render(): JSX.Element;
    /** Gets references to field DOM elements */
    private getFields;
    private getFieldValues;
    /** Gets the previous step */
    private getPreviousStep;
    /** Gets the next step */
    private getNextStep;
    /** Gets all the steps in this wizard */
    private getSteps;
    /** Gets the current step */
    private getCurrentStep;
    /**
     * Get a step by its name
     * @param {string} name - the step name property
     */
    private getStep;
    /** Deactivates all the steps */
    private deactivateSteps;
    /** Activates a step */
    private activateStep;
    /** Sets the height on all the steps */
    private setHeightOnSteps;
}
