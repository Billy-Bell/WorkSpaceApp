import '../../stencil.core';
export declare class TagFilePicker {
    /**
     * The id of the file input
     * This is important if there are multiple uploads on the page
     */
    id: string;
    /** Whether multiple files can be uploaded */
    multiple: boolean;
    /** Whether a drag drop area is rendered */
    allowDragDrop: boolean;
    /** Whether a large drag drop area is rendered */
    large: boolean;
    /** The message to display in the drop area  */
    dragDropMessage: string;
    /** The default text to display on the button */
    buttonText: string;
    /** The template text to display when files have been selected */
    multipleFilesCaption: string;
    /** Whether the component is disabled */
    disabled: boolean;
    /** Event raised when a file is selected  */
    private fileChange;
    /** Whether the DOM element */
    private element;
    /** The text to display on the button */
    private displayText;
    /** The class to display for the drag container when a drag drop is occurring */
    private dragClass;
    /** The files that have been selected */
    private selectedFiles;
    /**
     * Sets the focus to the file picker button
     */
    focusButton(): void;
    render(): JSX.Element;
    private getLabelText;
    private renderClear;
    private clearSelection;
    private renderFileInput;
    private handleFileUploadChange;
    private handleDragEnter;
    private handleDragLeave;
    private handleDragOver;
    private handleDrop;
    private getFileDisplayText;
    private raiseFileChangeEvent;
}
