import '../../stencil.core';
import { EventEmitter } from '../../stencil.core';
import { IRecordRequestArgs } from '../../shared/types';
export declare class TagRecordNav {
    /** The current record number  */
    record: number;
    /** The total number of record  */
    recordCount: number;
    /** Event raised when a record number is clicked  */
    recordRequest: EventEmitter<IRecordRequestArgs>;
    render(): JSX.Element;
    /** Gets the array of record numbers */
    private getRecordArray;
    private handleClick;
}
