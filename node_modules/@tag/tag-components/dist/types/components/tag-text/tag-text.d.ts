import '../../stencil.core';
import { Accent, DataType, TextType } from '../../shared/types';
export declare class TagText {
    /** The type of the string  */
    type: TextType;
    /** The text  */
    text: string;
    /** Treat text as inline and remove block and margin from text */
    inline?: boolean;
    /** The colour of the text */
    accent?: Accent;
    /** The styles to apply to the text */
    textStyle: string;
    /** The type of the field that is used for value formatting */
    dataType?: DataType;
    /** The formatting options */
    formattingOptions?: string;
    render(): JSX.Element;
    private getClassName;
    private getStyle;
}
