import '../../stencil.core';
import { Accent } from '../../shared/types';
export declare class TagTabs {
    /** The type of tabs to render */
    type: 'block' | 'underline';
    /** JSX style object in JSON to apply to the tab header */
    headerStyle: string;
    /** JSX style object in JSON to apply to the tab content */
    panelStyle: string;
    /** The accent colour for the selected tab */
    accent: Accent;
    /** Reference to the DOM element */
    private element;
    /** Internal state for each tab */
    private tabs;
    /** Event raised when the active tab changes */
    private tabChange;
    /** Gets the child tab components */
    componentDidLoad(): void;
    /**
     * Selects a tab
     * @param {string} tabCaption - The caption of the tab to select
     */
    select(key: string): void;
    render(): JSX.Element;
    /** Gets the tabs object from the tag-tab child components */
    private getTabsObject;
    /** Handles when a tab is clicked */
    private handleTabClick;
    /** Renders a tab header */
    private renderTab;
    /**
     * Shows the selected tab content.
     * Note that all the tabs content is rendered
     * - this method simply shows or hides the content.
     */
    private showSelectedTabContent;
}
