export interface IDay {
    weekDayName: string;
    weekDayNumber: number;
    number: number;
    date: Date;
    inMonth: boolean;
}
export interface IDayRow {
    days: IDay[];
}
export interface IDatePickerChangeArgs {
    value: Date;
}
/**
 * Gets the day state for a given month
 * @param year - The year
 * @param month - The month
 * @param firstDayOfWeek - The first day number of the week
 */
export declare const getDayRows: (year: number, month: number, firstDayOfWeek: number) => IDayRow[];
