import '../../stencil.core';
import { Accent, Icon } from '../../shared/types';
export declare class TagAppHeader {
    /** The accent colour for top border of the header as well as the icon */
    accent: Accent;
    /** The icon for the app */
    icon: Icon;
    /** The colour of the icon background */
    iconAccent: Accent;
    /** The title for the app */
    heading: string;
    /** The colour of the title text */
    headingAccent: Accent;
    private element;
    private itemsVisible;
    componentDidLoad(): void;
    componentDidUnload(): void;
    /**
     * Subscribe to the menuItemClicked event from the menu items.
     * When menu items are clicked we should hide the menu
     */
    menuItemClickHandler(): Promise<any>;
    render(): JSX.Element;
    private getIconAccentClassName;
    private handleMenuClick;
    private hideMenu;
}
