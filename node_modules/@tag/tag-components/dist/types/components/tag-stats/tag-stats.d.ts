import '../../stencil.core';
import { Accent } from '../../shared/types';
export declare class TagStats {
    /** The heading */
    heading: string;
    /** The sub heading */
    subHeading: string;
    /** The text colour */
    accent: Accent;
    /** The field to get the labels from */
    labelField: string;
    /** The field to get the values from */
    valueField: string;
    /** The data */
    data: any[];
    render(): JSX.Element;
    private renderLabelAndValue;
}
