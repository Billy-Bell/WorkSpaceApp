import { IHttpRequest, IHttpResponse } from '../../shared/http';
export declare type RequestMapper = (request: IHttpRequest) => IHttpRequest;
export declare type ResponseMapper = (response: IHttpResponse) => IHttpResponse;
export interface IPagedData {
    data: any[];
    page?: number;
    pageSize?: number;
    pageCount?: number;
    recordCount?: number;
    error?: boolean;
}
/**
 * Maps response data into IPagedData
 * @param {any[]} data - The response body data
 */
export declare const responseCollectionMapper: (response: IHttpResponse) => IPagedData;
